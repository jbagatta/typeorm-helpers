{
  "name": "typeorm-helpers",
  "version": "1.0.0",
  "description": "useful type magic for working with typeorm",
  "main": "src/index.ts",
  "types": "./build/index.d.ts",
  "scripts": {
    "clean": "rm -r ./build",
    "build": "tsc -b",
    "test": "jest --silent ./tests/unit/**/*"
  },
  "author": "John Bagatta",
  "license": "ISC",
  "dependencies": {
    "typeorm": "^0.3.22"
  },
  "devDependencies": {
    "@types/node": "^13.13.5",
    "typescript": "^4.4.4"
  }
}
